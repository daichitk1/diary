<p style="color: green"><%= notice %></p>

<% content_for :title, "One diaries" %>

<h1>日記一覧</h1>

<div id="one_diaries">
  <% @one_diaries.each do |one_diary| %>
    <%= render one_diary %>
    <p>
      <%= link_to "Show this one diary", one_diary %>
    </p>
  <% end %>
</div>

<%= link_to "New one diary", new_one_diary_path %>
