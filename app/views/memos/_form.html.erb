<%= form_with(model: memo) do |form| %>
  <% if memo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(memo.errors.count, "error") %> prohibited this memo from being saved:</h2>

      <ul>
        <% memo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="m-3">
    <%= form.label :title, "タイトル", style: "display: block" %>
    <%= form.text_field :title, class: "form-control title" %>
  </div>

  <div class="m-3">
    <%= form.label :content, "内容", style: "display: block" %>
    <%= form.text_area :content, class: "form-control text" %>
  </div>

  <div class="m-3">
      <%= form.submit '決定', class: "btn btn-outline-primary" %>
  </div>
<% end %>
